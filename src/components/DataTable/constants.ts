export const ACTIONS_COLUMN_ID = '__actions' as const;

export const MEMOIZED_HANDLE_ID = Symbol();

export const ROW_ACTIONS_METADATA_KEY = Symbol();

export const TABLE_NAME_METADATA_KEY = Symbol();
